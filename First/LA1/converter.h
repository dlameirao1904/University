/**
 * @file que guarda as defenições das funcoes usadas no file converter.c
*/

#ifndef CONVERTER_H
#define CONVERTER_H
#include "functions.h"
#include <stdlib.h>

long toLong(DATA x);
double toDouble(DATA x);
char toChar(DATA x);
char* toString(DATA x);


#endif
